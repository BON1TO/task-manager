{
  "name": "taskflow-root",
  "version": "1.0.0",
  "description": "Root package for TaskFlow",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
